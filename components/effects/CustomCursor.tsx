// Custom cursor removed — browser default cursor is used for performance.
// The laggy dual-spring + MutationObserver approach caused noticeable jank.
export default function CustomCursor() {
  return null;
}
